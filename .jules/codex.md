## 2026-05-21 - README Code Snippet Discrepancies and Typo Correction
**Observation:** The README.md contained an epistemological deficit concerning the generated C# source code. Specifically, a structural property assignment typo existed (`Z = value.Y; Z = value.Z;` instead of `Y = value.Y; Z = value.Z;`). Additionally, the documentation inaccurately claimed that preprocessor directives such as `#if !RO_GEN` were entirely omitted from the generated struct, when empirical testing reveals that the Roslyn source generator retains trivia (including preprocessor directives). The directives are present in the output code, merely deactivated by `#define RO_GEN` at the file's commencement.
**Strategic Action:** Synchronized the documentation to accurately reflect both the property assignments and the structural retention of preprocessor directives. Code examples must be perfectly deterministic and epistemologically sound.

## 2026-06-21 - Documentation Parity Sync
**Observation:** The README.md examples for generated code did not strictly match the actual emitted output from `StructCopyGenerator.cs`. Specifically, the generator emits `// This file was generated by Tedd.StructCopyGenerator` before the block-scoped namespace, retains the original struct's attributes (including `[GenerateReadOnlyStruct]`), and wraps `using` directives inside the block-scoped namespace. Furthermore, the examples lacked modern .NET features like file-scoped namespaces on the input structs.
**Strategic Action:** Synchronized the README.md code examples. Updated input structs to use file-scoped namespaces (`namespace Example;`). Updated output snippets to include the generator comment, the block-scoped namespace, the retained attribute `[GenerateReadOnlyStruct]`, and proper placement of `using System.Numerics;` within the namespace block to precisely map to the source generator's deterministic output.

## 2025-02-18 - Documentation Drift and Architectural Clarification
**Observation:** The README.md lacked a formal articulation of the internal mechanics, which strictly rely on a Roslyn compile-time source generator pipeline without runtime components like hierarchical data binding or routed events. Additionally, while updating the README, verified that constructors are correctly documented as generated by default, and `[GenerateReadOnlyStruct(false, false)]` disables them.
**Strategic Action:** Appended an `Architectural Paradigms` section to README.md detailing the Roslyn generator pipeline structure and clarifying the lack of runtime structural components, ensuring accurate alignment with the framework's mechanics.
